# Lutris

`sudo pacman -S lutris` — install all optional deps. If needed: `sudo pacman -S vulkan-devel`. BattleNet: use installer script from Lutris site and follow its instructions. Vulkan “not available” can persist (used to work on Manjaro); check drivers and env.
